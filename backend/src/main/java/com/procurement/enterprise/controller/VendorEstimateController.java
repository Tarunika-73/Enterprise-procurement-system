package com.procurement.enterprise.controller;

import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

/**
 * TODO: implement endpoints for VendorEstimateController.
 * Placeholder created to match the agreed package structure.
 */
@RestController
@RequestMapping("/api/vendorestimates")
public class VendorEstimateController {

    @GetMapping
    public String placeholder() {
        return "VendorEstimateController is not yet implemented";
    }
}
