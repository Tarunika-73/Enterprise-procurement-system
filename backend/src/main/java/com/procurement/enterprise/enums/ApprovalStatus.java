package com.procurement.enterprise.enums;

/**
 * Maps the SQL ENUM for {@code approvals.status}.
 * Values: 'Pending', 'Approved', 'Rejected', 'Escalated'
 */
public enum ApprovalStatus {
    PENDING,
    APPROVED,
    REJECTED,
    ESCALATED
}
