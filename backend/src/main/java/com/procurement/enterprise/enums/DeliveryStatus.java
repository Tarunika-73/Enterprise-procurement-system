package com.procurement.enterprise.enums;

/**
 * Maps the SQL ENUM for {@code deliveries.status}.
 * Values: 'Pending', 'In Transit', 'Delivered', 'Failed'
 */
public enum DeliveryStatus {
    PENDING,
    IN_TRANSIT,
    DELIVERED,
    FAILED
}
