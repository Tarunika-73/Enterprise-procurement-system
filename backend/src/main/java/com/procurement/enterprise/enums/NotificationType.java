package com.procurement.enterprise.enums;

/**
 * Maps the SQL ENUM for {@code notifications.type}.
 * Values: 'Email', 'SMS', 'System', 'Push'
 */
public enum NotificationType {
    EMAIL,
    SMS,
    SYSTEM,
    PUSH
}
