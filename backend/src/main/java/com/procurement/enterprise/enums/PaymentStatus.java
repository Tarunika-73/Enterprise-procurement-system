package com.procurement.enterprise.enums;

/**
 * Maps the SQL ENUM for {@code payments.status}.
 * Values: 'Pending', 'Partially Paid', 'Paid', 'Cancelled'
 */
public enum PaymentStatus {
    PENDING,
    PARTIALLY_PAID,
    PAID,
    CANCELLED
}
