package com.procurement.enterprise.enums;

/**
 * Maps the SQL ENUM for {@code purchase_orders.status}.
 * Values: 'Created', 'Sent', 'Accepted', 'Rejected', 'Delivered', 'Closed', 'Cancelled'
 */
public enum PurchaseOrderStatus {
    CREATED,
    SENT,
    ACCEPTED,
    REJECTED,
    DELIVERED,
    CLOSED,
    CANCELLED
}
